// Loads the registration page and starts its shared frontend application.
import { createPageApp } from '../app.js';
import { renderRegisterPage } from '../screens/registerPage.js';

createPageApp(renderRegisterPage);
