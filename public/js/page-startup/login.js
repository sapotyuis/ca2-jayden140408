// Loads the login page and starts its shared frontend application.
import { createPageApp } from '../app.js';
import { renderLoginPage } from '../screens/loginPage.js';

createPageApp(renderLoginPage);
