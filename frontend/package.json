{
  "name": "castaway-chronicles-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Castaway Chronicles — vanilla JavaScript frontend (Vite). Talks to the Express API in the parent folder.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.1"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
